# Dependencias e artefactos (se instalan/generan dentro del build)
node_modules
.next
dist
out
*.tsbuildinfo

# Secretos y entornos locales
.env
.env.*
!.env.example

# VCS / editor / OS
.git
.gitignore
.idea
.vscode
.DS_Store
Thumbs.db

# Docker / compose / infra (no necesarios dentro de la imagen)
Dockerfile
.dockerignore
docker-compose*.yml
Caddyfile
backups
docker/data

# Logs y coverage
*.log
coverage
.nyc_output
playwright-report
test-results
